# 离线商业效果展厅

这个静态应用只读取交付包内的 4K 主图和 8K 等距柱状全景，不依赖 CDN、在线
接口或开发服务器。桌面端支持鼠标拖动与滚轮缩放，手机和平板支持单指旋转与
双指缩放；显卡无法载入 8K WebGL 纹理时会自动显示普通全景图。

正式交付包由仓库根目录执行：

```bash
python3 tools/cinematic/package_showroom.py
```

打包器会校验正式渲染文件、生成浏览版 JPEG、保留 4K/8K PNG 母版、编译原生
macOS 启动器，并为全部文件生成 SHA-256 清单和确定性 ZIP。
