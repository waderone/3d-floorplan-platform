# 离线商业效果素材

此目录只保存可审计的素材清单和许可证，不把约 190 MiB 的 4K 原始素材直接
提交进普通 Git。使用以下命令下载并校验到忽略目录：

```bash
python3 tools/cinematic/prepare_assets.py
```

默认输出为 `work/cinematic-assets`。下载器逐文件校验官方 API 冻结的字节数与
MD5；Blender 主场景只读取校验通过的文件。

清单是已审计候选池，不等同于最终画面必须使用的物件。生成器会根据真实门洞和
通行净空排除摆放不成立的候选；例如本基准最终删除了楼梯通道内的单椅与大盆栽。
